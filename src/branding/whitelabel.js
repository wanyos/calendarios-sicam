// Config de branding para el build white-label (mode "whitelabel").
// Mismo shape que sicam.js — `enabled: false` indica que NO se inyecta
// el bloque branded (logo en hero, eyebrow, texto de footer).
// El `title` siempre debe existir porque va a la pestaña del navegador.

export default {
    enabled:     false,
    title:       'Calendarios',
    eyebrow:     null,
    footerText:  null,
    logoSrc:     null,
    logoAlt:     null,
}
